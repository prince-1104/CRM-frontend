import { uniformRouteMetadata, UniformCategoryPage } from "../../lib/UniformCategoryPage";

export const metadata = uniformRouteMetadata("catering-uniforms");

export default function Page() {
  return <UniformCategoryPage slug="catering-uniforms" />;
}
